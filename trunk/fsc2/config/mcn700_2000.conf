/* -*-C-*-
  $Id$

  This file contains configuration information for controlling the F.u.G.
  Elektronik GmbH MCN700-2000 power supply. This file will be included as
  a header file, so the syntax must be valid C and changes will only become
  visible after re-compilation of the module.
*/


/* Define the name that's going to be used for the device. It must be
   identical to the name used in the GPIB configuration file (usually
   /etc/gpib.conf). */

#define DEVICE_NAME     "MCN700_2000"


/* Define the generic type of the device */

#define DEVICE_TYPE     "power supply"


/*
 * Local variables:
 * tab-width: 4
 * indent-tabs-mode: nil
 * End:
 */
