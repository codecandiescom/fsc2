/* -*-C-*-
  $Id$

  This file contains configuration information for controlling the 
  Rulbus 12-bit DAC card RB9510. This file will be included as a
  header file, so the syntax must be valid C and changes will only
  become visible after re-compilation of the module.
*/

/* Define the name that's going to be used for the device */

#define DEVICE_NAME     "RB8510_1"


/* Define the generic type of the device */

#define DEVICE_TYPE     "daq"


/* Define the Rulbus card name (must be identical to the name set in the
   Rulbus configuration file, which usually is '/etc/rulbus.conf') */

#define RULBUS_CARD_NAME  "DAC12_1"


/*
 * Local variables:
 * tab-width: 4
 * indent-tabs-mode: nil
 * End:
 */
