VARS:

N = 100;
M = 100;
K;


PREP:

init_2d( 2, 0, 0, "Time [ns]", "Field [mT]", "I forgot what this is..." );

EXP:

set_seed( 23423 );

FOR K = 1:10000 {
	display( ceil( N * random( ) ), ceil( M * random( ) ), random( ) );
	wait( 150 ms );
}
