VARIABLES:

X = 3,
Y = 5,
Z,
R[ 4 ] = { 1, 2, 3, 4 },
r;

sf = 8 MHz;
ef = 22 MHz;
fs = 24.9999 kHz;


PREPARATIONS:

print( "\T#, #, #\n", ( ef - sf ) / fs, int( ( ef - sf ) / fs ),
		ceil( ( ef - sf ) / fs ) );

Z = X + exp( Y ms );

print( "\TZ = #\n", Z );
print( "\Ta = #\n", exp( - 1000000 ) );

EXPERIMENT:
save( R );
