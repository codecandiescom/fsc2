/*
  $Id$
*/


#if ! defined FUNC_BASIC_HEADER
#define FUNC_BASIC_HEADER


#include "fsc2.h"


Var *f_int(     Var *v );
Var *f_float(   Var *v );
Var *f_round(   Var *v );
Var *f_floor(   Var *v );
Var *f_ceil(    Var *v );
Var *f_abs(     Var *v );
Var *f_sin(     Var *v );
Var *f_cos(     Var *v );
Var *f_tan(     Var *v );
Var *f_asin(    Var *v );
Var *f_acos(    Var *v );
Var *f_atan(    Var *v );
Var *f_sinh(    Var *v );
Var *f_cosh(    Var *v );
Var *f_tanh(    Var *v );
Var *f_exp(     Var *v );
Var *f_ln(      Var *v );
Var *f_log(     Var *v );
Var *f_sqrt(    Var *v );
Var *f_random(  Var *v );
Var *f_setseed( Var *v );
Var *f_time(    Var *v );
Var *f_dtime(   Var *v );
Var *f_date(    Var *v );
Var *f_dim(     Var *v );
Var *f_size(    Var *v );
Var *f_sizes(   Var *v );
Var *f_mean(    Var *v );
Var *f_slice(   Var *v );


#endif  /* ! FUNC_BASIC_HEADER */
