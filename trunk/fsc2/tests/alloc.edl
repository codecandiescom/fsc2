VARIABLES:

C = 3;
c = 1.2;
D[ 5 ];
d[ 6 ];
E[ * ];
e[ * ];
M[ * ];
m[ * ];
N[ 2, 7 ];
n[ 3, 4 ];
H[ C, * ];
h[ size( N, 1 ), * ];
I[ * ] = { 1, 2, C };
i[ * ] = { 1.0, 2.0, 3.0 };
k[ * ];

PREPARATIONS:

E = D;
e = d;
H[ 1 ] = I;
h[ 2 ] = i;

I += C;
i *= c;

M = slice( D, 2, 2 );
m = slice( d, 5, 2 );

m += M;

mean( h[ 1 ] );

k = sizes( h );