/* -*-C-*-
  This file contains configuration information for the Gentec
  Maestro power/energy meter, controlled either via LAN or USB.
  This file will be included as a header file, so the syntax must
  be valid C and changes will only become active after re-compilation
  and re-installation of the module.
*/


/* Define the name that's going to be used for the device. */

#define DEVICE_NAME     "GENTEC_MAESTRO"


/* Define the generic type of the device */

#define DEVICE_TYPE     "powermeter"


/* This module can be be compiled to either control the device
   via USB or Ethernet. Please define either 'USE"USB' or 'USE_LAN'
   (but not both), together with the required parameters. Note that
   control via USB is just a serial connection via a USB-to-serial
   adaptor, so the parameters for the "USB connection" look exactly
   line for a serial port connection. */

#define USE_USB

/* Define the device file of the uSB connection */

#define DEVICE_FILE  "/dev/ttyUSB0"


/* Define the baud rate for communication with the gaussmeter. Use only
   the constants mentioned in the man page for termios(3). */

#define SERIAL_BAUDRATE B115200

/* Define the number of stop bits */

#define NUMBER_OF_STOP_BITS  1


/* Define the number of bits per character */ 

#define NUMBER_OF_BITS_PER_CHARACTER  8


/* Define parity to be used, it can be either 'NO_PARITY', ODD_PARITY' or
   'EVEN_PARITY' */

#define PARITY_TYPE  NO_PARITY


#if 0

#define USE_LAN


/* Define the network address of the device (as a string), either in
 * quad-dotted form or as a name that can be resolved via a DNS request */

#define NETWORK_ADDRESS  "192.168.99.45"


/* Define the port the device is listening on */

#define PORT 5000

#end


/* Set the maximum timeout when trying to open the connection to the
   device, reading from it and write to it (in microseconds, if det to
   0 no timeout is used) */

#define OPEN_TIMEOUT      500000    /* 100 ms */
#define READ_TIMEOUT      100000    /* 100 ms */
#define WRITE_TIMEOUT     100000    /* 100 ms */


/*
 * Local variables:
 * tab-width: 4
 * indent-tabs-mode: nil
 * End:
 */
